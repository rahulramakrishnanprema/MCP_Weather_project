from setuptools import setup, find_packages

setup(
    name='mcp_weather_project',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        'langchain',
        'langgraph',
        'transformers',
    ],
    python_requires='>=3.9',
)
from setuptools import setup, find_packages

setup(
    name='mcp_weather_project',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        'langchain',
        'langgraph',
        'transformers',
    ],
    python_requires='>=3.9',
)



